#include "obliczenia.h"
double obliczenia::suma(double MEM, double B) {
        return MEM + B;
	};
/*double obliczenia::roznica(double MEM, double B) {
		std::cout << "Druga liczba: ";
		std::cin >> B;
		return MEM = (MEM - B);
	};
double obliczenia::mnozenie(double MEM, double B) {
		std::cout << "Druga liczba: ";
		std::cin >> B;
		return MEM = (MEM * B);
	};
double obliczenia::dzielenie(double MEM, double B) {
		std::cout << "Druga liczba: ";
		std::cin >> B;
		if (B) {
			return MEM = (MEM / B);
		}
		else {
			std::cout << "dzielenie przez zero, try again: \n";
			return dzielenie();
		}
	};
double obliczenia::modulo(double MEM, double B) {
		std::cout << "Druga liczba: ";
		std::cin >> B;
		if (B) {
			return MEM = (MEM - (int)(MEM / B) * B);
		}
		else {
			std::cout << "dzielenie przez zero, try again: \n";
			return modulo();
		}
    };*/

